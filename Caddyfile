{$DOMAIN:localhost} {
    redir /metrics /metrics  # internal, not exposed
    handle_path /grafana/* {
        reverse_proxy grafana:3000
    }
    handle {
        reverse_proxy api:8000
    }
    log {
        output stdout
        format json
    }
}
